# 生产环境配置

# 应用标题
VITE_APP_TITLE=移动端应用

# API 基础路径（接口地址从 public/config.js 的 systemApi 读取，这里仅作为兜底）
VITE_APP_BASE_API=/

# 是否启用 Mock 数据（生产环境默认禁用）
VITE_USE_MOCK=false

# 是否启用 vconsole 调试（生产环境默认禁用）
VITE_USE_VCONSOLE=false

# 是否启用 source map
VITE_BUILD_SOURCEMAP=false
