# AI 交互组件清单与实施方案

## 1. 现有组件 (Existing Components)

### 基础聊天组件
- **Chat.vue**: 主聊天容器
- **ChatHeader.vue**: 聊天头部
- **ChatInput.vue**: 输入框组件
- **MessageItem.vue**: 消息项组件
- **TypingBubble.vue**: 打字动画效果
- **WelcomeSection.vue**: 欢迎区域
- **QuickQuestions.vue**: 快捷问题

### 功能性组件
- **UploadOptions.vue**: 上传选项
- **UploadPreview.vue**: 上传预览
- **BottomNavigation.vue**: 底部导航

### 公共组件
- **MarkdownRenderer.vue**: Markdown渲染器

---

## 2. 缺失组件规划 (Missing Components Plan)

### 🔥 高优先级 (立即实现)
| 组件名 | 用途 | 关键功能 |
| :--- | :--- | :--- |
| **MessageFeedback** | 消息反馈 | 点赞、踩、复制内容、重新生成 |
| **CodeBlock** | 代码块增强 | 语法高亮、一键复制、语言标识 |
| **ErrorMessage** | 错误提示 | 友好错误展示、重试按钮 |
| **StreamingController** | 流式控制 | 暂停/继续/停止生成、进度显示 |
| **MessageSkeleton** | 加载骨架屏 | 等待回复时的占位动画 |

### ⭐ 中优先级 (近期实现)
| 组件名 | 用途 | 关键功能 |
| :--- | :--- | :--- |
| **SuggestedPrompts** | 建议问题 | 智能推荐追问、动态刷新 |
| **MediaPreview** | 多模态预览 | 图片/视频/音频预览播放 |
| **VoiceInput** | 语音交互 | 语音转文字、文字转语音 |
| **CitationCard** | 引用来源 | 显示参考源、可信度 |

### 💡 低优先级 (按需实现)
| 组件名 | 用途 | 关键功能 |
| :--- | :--- | :--- |
| **ThinkingProcess** | 思维链展示 | 展开/折叠思考步骤 |
| **ContextManager** | 上下文管理 | 清空会话、历史记录 |
| **TableRenderer** | 表格渲染 | 响应式表格、导出CSV |
| **ContentCard** | 卡片内容 | 结构化数据展示 |
| **ChatSettings** | 对话设置 | 模型选择、参数调节 |

---

## 3. 实施路线图 (Implementation Roadmap)

### 阶段一：核心交互增强 (Current Focus)
1.  **MessageFeedback.vue**
    - 集成到 `MessageItem.vue`
    - 实现点赞/踩状态切换
    - 实现剪贴板复制功能
2.  **CodeBlock.vue**
    - 集成到 `MarkdownRenderer.vue` 或独立使用
    - 引入语法高亮库 (如 highlight.js)
3.  **ErrorMessage.vue**
    - 用于 `Chat.vue` 处理 API 错误
4.  **StreamingController.vue**
    - 用于 `ChatInput.vue` 或 `Chat.vue` 底部
5.  **MessageSkeleton.vue**
    - 替换简单的 loading 状态

### 阶段二：功能扩展
- 实现 `SuggestedPrompts` 提升对话连贯性
- 完善文件上传后的 `MediaPreview`

### 阶段三：高级特性
- 根据业务需求引入 `ThinkingProcess` 和 `VoiceInput`
